# GroceryListWebService

### Start localhost Web Service Server
Start a localhost server that starts on port 8080
```
 ./gradlew bootRun

```
Additionally, a Spring Boot Run Configuration can be created in the IDE

### Run Unit Tests

```
 ./gradlew test

```

